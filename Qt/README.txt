The program parses input function and computes integral of it on input interval.

Visual shell is created. Window is open.
User puts info mentioned above into corresponding boxes. Function can be in natural form (tg(x), not tan(x); )
Lexical analysis of the input expression (function). Tree of lexeme(tokens) is created.
Integral is computed.
The Window changes. The result is displayed
